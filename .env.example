#===========================================
# Серверные переменные окружения
#===========================================

# URI подключения к PostgreSQL базе данных
# Формат: postgresql://username:password@host:port/database?schema=schema_name
DATABASE_URI="postgresql://postgres:postgres@localhost:5432/example?schema=public"

# Секретный ключ для подписи/проверки payload (например, для JWT токенов)
PAYLOAD_SECRET="7#w#[u/5aHTA@uwN"

#===========================================
# Клиентские переменные окружения (NEXT_PUBLIC_)
#===========================================

# URL API для клиентской части приложения
# Используется в компонентах на стороне клиента
NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# Основной URL сайта
# Используется для формирования полных URL-адресов
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
